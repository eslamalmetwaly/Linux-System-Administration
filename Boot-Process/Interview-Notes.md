# Interview Questions

## Why do we use chroot?

To change the root directory to the installed operating system.

---

## What is initramfs?

A temporary root filesystem loaded into memory before the real root filesystem.

---

## Why do we use touch /.autorelabel?

To relabel SELinux contexts after changing the root password.